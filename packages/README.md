# landseries monorepo entry.
These are our monorepo setup.

- `@landseries/backend` for storing our backend code powered by `fastify`.
- `@landseries/common` for storing shared code throughout the project.
- `@landseries/frontend` for storing UI related code powered by `Vue`
